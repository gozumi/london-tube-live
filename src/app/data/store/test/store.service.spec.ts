import { Fixture } from './store.fixture';

describe('service: Store', () => {
    beforeEach(() => {
        this.storeService = new Fixture().storeService;
    });

    it('should ...', () => {
        expect(this.storeService).toBeTruthy();
    });
});
